import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mpp.mobile",
  appName: "MPP",
  webDir: "www",
  server: {
    androidScheme: "https",
  },
};

export default config;
